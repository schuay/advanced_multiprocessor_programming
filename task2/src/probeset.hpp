#ifndef __PROBESET_H
#define __PROBESET_H

#define PROBE_THRESHOLD (8)
#define PROBE_SIZE (16)

template <typename T>
class ProbeSet {
public:
    /* TODO. Wrap std::vector (or similar). */
};

#endif /* __PROBESET_H */
